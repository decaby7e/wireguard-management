version: 1

my_test_network:
  # entrypoint: entrypoint.example.com:7560  # Could be used for mesh networking at some point?
  
  hosts:
    my-personal-machine:
      pubkey: SuperNotSecretPubKey==
      # privkey: SuperSecretPrivateKey!!==  # Not such a good idea to have private keys in plaintext... Perhaps add encryption?
      routes:
        - 192.168.5.2/32
        
    my-server:
      pubkey: 
      routes: 192.168.5.1/32
      
