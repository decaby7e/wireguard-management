#!/bin/bash

INTERFACE_NUM=$1

if [ -d "host-$INTERFACE_NUM-registered" ]

then
  INTERFACE_CONF=host-$INTERFACE_NUM-registered/wg$INTERFACE_NUM.conf
  DATE=$(date +"%D %T")

  #GO THROUGH THE SPECIFIED INTERFACE FILE, FIND THE REGISTRATION STRING, AND REMOVE IT
  #GO THROUGH THE REGISTRATION FILE AND REMOVE THE INSTANCE OF THAT INTERFACE

  mv host-$INTERFACE_NUM-registered host-$INTERFACE_NUM

else
  echo "Error: Client is not registered"

fi

exit 0
